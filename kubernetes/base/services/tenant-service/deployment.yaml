apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-service
  namespace: saas-framework
  labels:
    app: tenant-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tenant-service
  template:
    metadata:
      labels:
        app: tenant-service
        version: v1
    spec:
      containers:
      - name: tenant-service
        image: ghcr.io/vhvplatform/go-tenant-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 50053
          name: grpc
          protocol: TCP
        - containerPort: 8083
          name: http
          protocol: TCP
        env:
        - name: MONGODB_URI
          valueFrom:
            configMapKeyRef:
              name: saas-config
              key: MONGODB_URI
        - name: MONGODB_DATABASE
          valueFrom:
            configMapKeyRef:
              name: saas-config
              key: MONGODB_DATABASE
        - name: ENVIRONMENT
          value: "production"
        - name: LOG_LEVEL
          value: "info"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8083
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: saas-framework
  labels:
    app: tenant-service
spec:
  type: ClusterIP
  ports:
  - port: 50053
    targetPort: 50053
    protocol: TCP
    name: grpc
  - port: 8083
    targetPort: 8083
    protocol: TCP
    name: http
  selector:
    app: tenant-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tenant-service-hpa
  namespace: saas-framework
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: tenant-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
