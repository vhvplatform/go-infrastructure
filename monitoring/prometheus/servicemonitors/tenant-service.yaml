apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tenant-service
  namespace: saas-framework
  labels:
    app: tenant-service
spec:
  selector:
    matchLabels:
      app: tenant-service
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
